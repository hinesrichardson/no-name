 This is a basic workflow to help you get started with Actions

name: CI
README.mdon:
  pull_request:
    branches: [ main ]
workflow_dispatch:
Spawn/dirt block distanc 50 from @player 
/@dirt block = model1 
  build:
    character=@modle2/=player:
    steps: = 2/when@button pushed @up arrow 
      stone block = spawn 6 block awway: 
      - uses: actions/checkout@v2
    stone block=@push player   
spawn = @ 20 block radius:
    block @dirt=push player: 
      - name: Run a multi-line script
        run: on /@cammand / side arrow pushed 
          echo Add other actions to build,
          echo test, and deploy your project.
